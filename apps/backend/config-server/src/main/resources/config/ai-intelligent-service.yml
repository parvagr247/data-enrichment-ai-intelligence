# ==============================================================================
# Centralized Configuration: AI Intelligent Service (Port 9742)
# ==============================================================================

server:
  port: 9742

spring:
  ai:
    google:
      genai:
        api-key: ${GEMINI_API_KEY:mock-key}
        chat:
          options:
            model: ${AI_MODEL:gemini-3.5-flash-lite}

ai:
  extraction:
    model: ${AI_MODEL:gemini-3.5-flash-lite}
    temperature: 0.1
    mock-mode: ${AI_MOCK_MODE:false}
